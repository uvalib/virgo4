<%# app/views/about/eds/_controls.html.erb -%>
<%-
  # Added controls for EBSCO EDS operations.
  available_controls =
    eds_controls({
      # NOTE: none currently
    }) if authorized?
  return unless available_controls.present?
-%>

<li class="sidebar-entry eds-controls">
  <%- available_controls.each_pair do |label, opt| -%>
    <%- options      = opt[:url] -%>
    <%- html_options = opt.except(:url) -%>
    <%= link_to(label, options, html_options) %>
  <%- end -%>
</li>
