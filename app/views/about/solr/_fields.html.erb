<%# app/views/about/solr/_fields.html.erb -%>
<%-
  # Tables of Solr fields grouped by Blacklight usage.
  template_opts = {
    repository: repository ||= :solr,
    fields:     fields     ||= @solr_fields || {},
    lens:       lens       ||= params[:lens]
  }
-%>
<%= render 'about/common/fields', template_opts %>
