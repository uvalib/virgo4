<%# app/views/catalog/_marc_view.html.erb -%>
<%-
  doc = document ||= @document
  marc_record = doc&.to_marc
  return unless marc_record.present?
-%>
<%= render_marc_record(marc_record, id: 'marc_view') %>
